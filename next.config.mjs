/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images:{
    domains: ["img.freepik.com", "images.oyoroomscdn.com", "www.shutterstock.com","encrypted-tbn0.gstatic.com"],
  },
};

export default nextConfig;
